apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - target:
      kind: ConfigMap
      name: meridian-e2e-settings
    patch: |-
      - op: replace
        path: /data/MERIDIAN_SCENARIO
        value: drop-traces
      - op: replace
        path: /data/MERIDIAN_DISABLE_TRACES
        value: "true"
