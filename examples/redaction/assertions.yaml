version: 2
defaults:
  timeout: 10s
  min_count: 1

fixtures:
  - redaction

contracts:
  - id: no-authorization-header
    severity: fail
    signal: traces
    expect:
      attributes_absent:
        - http.request.header.authorization
